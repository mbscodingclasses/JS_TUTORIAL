let box = document.querySelector("#box");//div

console.log("Box offsetTop : " + box.offsetTop);

console.log("Box offsetLeft : " + box.offsetLeft);

// id => select => exact top , left 

// tagname => select => exact top , left 

// className => select => exact top , left 




